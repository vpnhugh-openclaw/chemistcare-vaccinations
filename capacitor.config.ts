import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.chemistcare.prescriberos',
  appName: 'ChemistCare',
  webDir: 'dist'
};

export default config;
